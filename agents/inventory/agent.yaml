name: inventory
description: Product stock and warehouse availability.
container:
  image: ${AZURE_CONTAINER_REGISTRY_ENDPOINT}/inventory:${AZURE_BUILD_TAG}
  port: 8088
  resources: { cpu: 0.5, memory: 1Gi }
protocols: [responses]
env:
  - { name: MODEL_DEPLOYMENT, value: gpt-4o }
