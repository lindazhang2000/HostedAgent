name: interior-designer
description: Interior design and DIY recommendations.
container:
  image: ${AZURE_CONTAINER_REGISTRY_ENDPOINT}/interior-designer:${AZURE_BUILD_TAG}
  port: 8088
  resources: { cpu: 0.5, memory: 1Gi }
protocols: [responses]
env:
  - { name: MODEL_DEPLOYMENT, value: gpt-4o }
