name: cora
description: Public-facing Zava shopping assistant; routes to specialists.
container:
  image: ${AZURE_CONTAINER_REGISTRY_ENDPOINT}/cora:${AZURE_BUILD_TAG}
  port: 8088
  resources: { cpu: 0.5, memory: 1Gi }
protocols: [responses]
env:
  - { name: MODEL_DEPLOYMENT, value: gpt-4.1 }
